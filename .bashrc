eval "$(fzf --bash)"
NEWLINE=$'\n'
PROMPT="%n@%m:%~${NEWLINE}> "

alias ls="ls -la"

alias gs="git status"
alias gaa="git add -A"
alias gcm="git commit -m"
alias gp="git push"
alias gco="git checkout"
